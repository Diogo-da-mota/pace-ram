// Arquivo de configuração de ambiente para o Lovable
// ATENÇÃO: use apenas em desenvolvimento no Lovable. Não commite chaves sensíveis privadas.
// A chave ANON e a URL são públicas por design do Supabase no frontend.
window.__ENV__ = {
  // Preencha com seus valores de DEV para o Lovable
  // Exemplo de URL (placeholder):
  VITE_SUPABASE_URL: "https://example-project.supabase.co",
  // Exemplo de anon key (formato típico, NÃO é chave real):
  VITE_SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.examplePayload.exampleSignature"
};